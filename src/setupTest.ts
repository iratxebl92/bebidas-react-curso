import '@testing-library/jest-dom';
// import {afterAll, afterEach, beforeAll } from 'vitest';
// import {server} from './mocks/server'

// beforeAll(() => server.listen());
// afterEach(() => server.restoreHandlers()) //cada vez que se consuma un servicio se reinicia para no afectar a otros tests
// afterAll(() => server.close()) // cuando acaba todos los test cierra el servidor porque ya no lo vamos a necesitar